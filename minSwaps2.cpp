#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int minSwaps(vector<int> nums) {

    return 0;
}

int main(void) {
    vector<int> numbers = {2,4,5,1,3};
    cout << minSwaps(numbers) << endl;

    return 0;
}